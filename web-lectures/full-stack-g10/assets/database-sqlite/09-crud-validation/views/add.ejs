<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Book - Library System</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
</head>
<body>
  <nav class="navbar is-link" role="navigation">
    <div class="navbar-brand">
      <a class="navbar-item" href="/">
        <strong>📚 School Library System</strong>
      </a>
    </div>
  </nav>

  <section class="section">
    <div class="container" style="max-width: 600px;">
      <div class="level">
        <div class="level-left">
          <div class="level-item">
            <h1 class="title">📖 Add New Book</h1>
          </div>
        </div>
        <div class="level-right">
          <div class="level-item">
            <a href="/" class="button">← Back to List</a>
          </div>
        </div>
      </div>

      <!-- Validation Errors -->
      <% if (error && error.length > 0) { %>
        <div class="notification is-danger is-light">
          <button class="delete"></button>
          <p class="heading">⚠️ Please fix the following errors:</p>
          <ul>
            <% error.forEach(err => { %>
              <li><%= err %></li>
            <% }); %>
          </ul>
        </div>
      <% } %>

      <div class="box">
        <form method="POST" action="/add">
          <div class="field">
            <label class="label">Book Title *</label>
            <div class="control">
              <input 
                class="input <%= error && error.some(e => e.includes('Title')) ? 'is-danger' : '' %>" 
                type="text" 
                name="title" 
                placeholder="e.g., Harry Potter and the Sorcerer's Stone"
                value="<%= typeof formData !== 'undefined' ? formData.title || '' : '' %>"
                required>
            </div>
            <p class="help">The full title of the book (2-200 characters)</p>
          </div>

          <div class="field">
            <label class="label">Author *</label>
            <div class="control">
              <input 
                class="input <%= error && error.some(e => e.includes('Author')) ? 'is-danger' : '' %>" 
                type="text" 
                name="author" 
                placeholder="e.g., J.K. Rowling"
                value="<%= typeof formData !== 'undefined' ? formData.author || '' : '' %>"
                required>
            </div>
            <p class="help">Author's full name (2-100 characters)</p>
          </div>

          <div class="field">
            <label class="label">ISBN *</label>
            <div class="control">
              <input 
                class="input is-family-monospace <%= error && error.some(e => e.includes('ISBN')) ? 'is-danger' : '' %>" 
                type="text" 
                name="isbn" 
                placeholder="978-1234567890"
                pattern="978-\d{10}"
                value="<%= typeof formData !== 'undefined' ? formData.isbn || '' : '' %>"
                required>
            </div>
            <p class="help">ISBN-13 format: 978-XXXXXXXXXX (must be unique)</p>
          </div>

          <div class="columns">
            <div class="column">
              <div class="field">
                <label class="label">Total Copies *</label>
                <div class="control">
                  <input 
                    class="input <%= error && error.some(e => e.includes('Copies')) ? 'is-danger' : '' %>" 
                    type="number" 
                    name="copies" 
                    min="1" 
                    max="1000"
                    value="<%= typeof formData !== 'undefined' ? formData.copies || '1' : '1' %>"
                    required>
                </div>
                <p class="help">1-1000</p>
              </div>
            </div>

            <div class="column">
              <div class="field">
                <label class="label">Available Copies *</label>
                <div class="control">
                  <input 
                    class="input <%= error && error.some(e => e.includes('Available')) ? 'is-danger' : '' %>" 
                    type="number" 
                    name="available" 
                    min="0" 
                    max="1000"
                    value="<%= typeof formData !== 'undefined' ? formData.available || '1' : '1' %>"
                    required>
                </div>
                <p class="help">Must be ≤ total copies</p>
              </div>
            </div>
          </div>

          <div class="field">
            <label class="label">Category *</label>
            <div class="control">
              <div class="select is-fullwidth">
                <select name="category" required>
                  <option value="">-- Select Category --</option>
                  <option value="Fiction" <%= typeof formData !== 'undefined' && formData.category === 'Fiction' ? 'selected' : '' %>>Fiction</option>
                  <option value="Non-Fiction" <%= typeof formData !== 'undefined' && formData.category === 'Non-Fiction' ? 'selected' : '' %>>Non-Fiction</option>
                  <option value="Science" <%= typeof formData !== 'undefined' && formData.category === 'Science' ? 'selected' : '' %>>Science</option>
                  <option value="History" <%= typeof formData !== 'undefined' && formData.category === 'History' ? 'selected' : '' %>>History</option>
                  <option value="Biography" <%= typeof formData !== 'undefined' && formData.category === 'Biography' ? 'selected' : '' %>>Biography</option>
                  <option value="Reference" <%= typeof formData !== 'undefined' && formData.category === 'Reference' ? 'selected' : '' %>>Reference</option>
                </select>
              </div>
            </div>
          </div>

          <div class="field is-grouped">
            <div class="control">
              <button type="submit" class="button is-primary">
                <strong>💾 Save Book</strong>
              </button>
            </div>
            <div class="control">
              <a href="/" class="button is-light">Cancel</a>
            </div>
          </div>
        </form>
      </div>

      <div class="notification is-info is-light">
        <p class="heading">ℹ️ Validation Rules</p>
        <ul>
          <li><strong>Title:</strong> 2-200 characters, required</li>
          <li><strong>Author:</strong> 2-100 characters, required</li>
          <li><strong>ISBN:</strong> Must be 978-XXXXXXXXXX format, unique</li>
          <li><strong>Copies:</strong> 1-1000, required</li>
          <li><strong>Available:</strong> 0-1000, cannot exceed total copies</li>
          <li><strong>Category:</strong> Must select from predefined list</li>
        </ul>
        <p class="mt-3"><strong>Note:</strong> All validation happens on the server before saving to the database.</p>
      </div>
    </div>
  </section>

  <script>
    // Auto-dismiss notification
    document.addEventListener('DOMContentLoaded', () => {
      const deleteButtons = document.querySelectorAll('.notification .delete');
      deleteButtons.forEach(button => {
        button.addEventListener('click', () => {
          button.parentElement.remove();
        });
      });
    });
  </script>
</body>
</html>
