# Quiz: JSON API Audit

## Maria wants to understand an existing JSON API before building a frontend for it. What is the first thing she should do?
* [ ] Start coding the UI immediately
* [x] Read the API documentation and test sample requests to understand the endpoints, parameters, and response structure
* [ ] Download all the data to a CSV file
* [ ] Ask her teacher for the answers

## What does this JSON response represent?
```json
{
  "id": 1,
  "name": "Juan Dela Cruz",
  "section": "Einstein"
}
```
* [ ] An array with three elements
* [x] A single object with three key-value pairs
* [ ] A CSV file
* [ ] An SQL query

## Carlo wants to know what data an API returns. He sends a test request using a tool like Postman or `fetch()` in the console. What is this process called?
* [ ] Deployment
* [x] API testing / endpoint exploration
* [ ] Compilation
* [ ] Debugging

## Liza is choosing between building a JSON API backend vs. traditional server-rendered pages (EJS) for her Grade 10 project. When is a JSON API the BETTER choice?
* [ ] When she wants the simplest possible setup
* [x] When she needs a mobile app or a separate frontend (like React) to consume the same data
* [ ] When the app has no user input
* [ ] When she doesn't know JavaScript

## When auditing a JSON API, what does the HTTP status code `200` mean?
* [ ] The server is down
* [x] The request was successful
* [ ] The resource was not found
* [ ] There was a server error

## An API ___ is a specific URL pattern that the API responds to, such as `GET /api/products` or `POST /api/students`.
Answer: endpoint
Answer: endpoints
Answer: route

## What does this HTTP status code indicate?
```
404 Not Found
```
* [ ] The request was successful but the data is empty
* [ ] The server crashed
* [x] The requested URL/resource does not exist on the server
* [ ] The user is not authorized

## Juan is deciding whether to build his first CRUD app as a JSON API or with EJS server-rendered pages. His teacher recommends starting with EJS. Why?
* [ ] EJS is faster in production
* [x] Server-rendered pages are simpler for beginners — one codebase handles both data and HTML, with no need for separate frontend JavaScript to fetch and render data
* [ ] JSON APIs are deprecated
* [ ] EJS is required by the Department of Education
