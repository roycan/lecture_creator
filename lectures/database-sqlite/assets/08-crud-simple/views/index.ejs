<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Inventory - CRUD Demo</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
</head>
<body>
  <nav class="navbar is-success" role="navigation">
    <div class="navbar-brand">
      <a class="navbar-item" href="/">
        <strong>🏪 Sari-Sari Store Inventory</strong>
      </a>
    </div>
  </nav>

  <section class="section">
    <div class="container">
      <div class="level">
        <div class="level-left">
          <div class="level-item">
            <div>
              <h1 class="title">📦 Product Inventory</h1>
              <p class="subtitle">Complete CRUD Operations</p>
            </div>
          </div>
        </div>
        <div class="level-right">
          <div class="level-item">
            <a href="/add" class="button is-primary is-medium">
              <strong>+ Add Product</strong>
            </a>
          </div>
        </div>
      </div>

      <!-- Stats -->
      <div class="columns">
        <div class="column">
          <div class="box has-background-info-light">
            <p class="heading">Total Products</p>
            <p class="title"><%= products.length %></p>
          </div>
        </div>
        <div class="column">
          <div class="box has-background-success-light">
            <p class="heading">Total Inventory Value</p>
            <p class="title">₱<%= total.toFixed(2) %></p>
          </div>
        </div>
        <div class="column">
          <div class="box has-background-warning-light">
            <p class="heading">Low Stock Items</p>
            <p class="title"><%= products.filter(p => p.stock < 20).length %></p>
          </div>
        </div>
      </div>

      <!-- Products Table -->
      <div class="box">
        <table class="table is-fullwidth is-striped is-hoverable">
          <thead>
            <tr>
              <th>ID</th>
              <th>Product Name</th>
              <th>Price</th>
              <th>Stock</th>
              <th>Category</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if (products.length === 0) { %>
              <tr>
                <td colspan="6" class="has-text-centered has-text-grey">
                  No products yet. Click "Add Product" to get started!
                </td>
              </tr>
            <% } %>
            
            <% products.forEach(product => { %>
              <tr>
                <td><%= product.id %></td>
                <td><strong><%= product.name %></strong></td>
                <td>₱<%= product.price.toFixed(2) %></td>
                <td>
                  <span class="tag <%= product.stock < 20 ? 'is-danger' : 'is-success' %>">
                    <%= product.stock %> units
                  </span>
                </td>
                <td>
                  <span class="tag is-info"><%= product.category %></span>
                </td>
                <td>
                  <div class="buttons">
                    <a href="/edit/<%= product.id %>" class="button is-small is-info">
                      ✏️ Edit
                    </a>
                    <form method="POST" action="/delete/<%= product.id %>" style="display: inline;" 
                          onsubmit="return confirm('Delete <%= product.name %>?')">
                      <button type="submit" class="button is-small is-danger">
                        🗑️ Delete
                      </button>
                    </form>
                  </div>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>

      <div class="notification is-info is-light">
        <p class="heading">💡 CRUD Operations</p>
        <ul>
          <li><strong>C</strong>reate - Add new products</li>
          <li><strong>R</strong>ead - View product list (this page)</li>
          <li><strong>U</strong>pdate - Edit existing products</li>
          <li><strong>D</strong>elete - Remove products</li>
        </ul>
        <p class="mt-3">All operations use <strong>prepared statements</strong> to prevent SQL injection attacks! 🛡️</p>
      </div>
    </div>
  </section>
</body>
</html>
