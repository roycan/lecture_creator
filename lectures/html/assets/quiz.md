# Quiz: HTML Fundamentals

## Ana is building a barangay officials page. She wants each official to have their own self-contained card with name, position, and photo. Which HTML element is the most semantic choice for each card?
* [ ] `<section>`
* [ ] `<div>`
* [x] `<article>`
* [ ] `<aside>`

## Carlo wants to make a navigation menu at the top of his sari-sari store website. Which element is designed specifically for navigation links?
* [ ] `<header>`
* [x] `<nav>`
* [ ] `<menu>`
* [ ] `<links>`

## Maria's contact form requires users to type a long message like a complaint or request. Which form element should she use for multi-line text input?
* [ ] `<input type="text">`
* [x] `<textarea>`
* [ ] `<input type="longtext">`
* [ ] `<input type="area">`

## Juan created a form with this code. When he clicks the submit button, nothing happens — the form doesn't send data. What is the bug?
```html
<form>
  <input type="text" name="email">
  <button type="reset">Submit</button>
</form>
```
* [ ] The `name` attribute is wrong
* [ ] The form needs a `<fieldset>`
* [x] The button type is `reset` instead of `submit`
* [ ] The input type should be `email`

## A classmate asks you: "Why should I use semantic HTML instead of just using `<div>` for everything?" Which is the best answer?
* [x] Semantic elements improve accessibility for screen readers and help search engines understand the page structure
* [ ] Semantic elements make the page load faster
* [ ] Semantic elements are required by HTML5 or the page will not display
* [ ] `<div>` tags are deprecated in HTML5

## Liza wants an image on her school project page to resize automatically to fit smaller phone screens. Which attribute should she add to her `<img>` tag?
* [ ] `size="auto"`
* [x] `max-width: 100%` (using CSS)
* [ ] `responsive="true"`
* [ ] `scale="fit"`

## For accessibility, every image should have an `___` attribute that describes what the image shows, so screen readers can read it aloud.
Answer: alt
Answer: alt attribute

## Ben's sari-sari store price list has 50 products. He wants to show them in rows and columns with headers like "Product", "Price", and "Stock". What HTML element is designed for this kind of tabular data?
* [ ] `<grid>`
* [ ] `<list>`
* [x] `<table>`
* [ ] `<matrix>`

## What does this HTML render in the browser?
```html
<a href="#contact">Jump to Contact</a>
```
* [ ] A link that opens the page "contact.html"
* [x] A link that scrolls to an element with `id="contact"` on the same page
* [ ] A link that opens the user's email client
* [ ] A link that downloads a file named "contact"

## Every HTML document must start with this declaration so the browser knows it is an HTML5 page. What is it?
Answer: <!DOCTYPE html>
Answer: DOCTYPE html
Answer: <!doctype html>
