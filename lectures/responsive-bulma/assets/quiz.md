# Quiz: Responsive Design with Bulma CSS

## Maria wants to use Bulma CSS in her project. What is the simplest way to add it to her HTML page without downloading any files?
* [ ] `npm install bulma` and import it
* [x] Add a `<link>` tag pointing to the Bulma CDN URL
* [ ] Write `@use bulma` at the top of her CSS file
* [ ] Copy the Bulma source code into a `<style>` tag

## Carlo wants to create a centered column of content on his page using Bulma. Which two container classes does he need to wrap his content?
* [ ] `<div class="wrapper">` and `<div class="center">`
* [x] `<div class="section">` and `<div class="container">`
* [ ] `<div class="page">` and `<div class="main">`
* [ ] `<div class="grid">` and `<div class="cell">`

## Liza wants three equal-width columns on desktop using Bulma's column system. What classes does she need on each column?
* [ ] `class="col-3"`
* [x] `class="column is-one-third"`
* [ ] `class="col-third"`
* [ ] `class="column third"`

## A classmate asks: "Why use Bulma instead of writing all the CSS ourselves?" Which is the strongest reason for a Grade 10 project?
* [ ] Bulma is required by HTML5
* [ ] Bulma makes the page load faster than custom CSS
* [x] Bulma provides pre-built, tested responsive components so you can build a good-looking, mobile-friendly layout much faster
* [ ] You cannot build responsive layouts without Bulma

## Juan wrote this Bulma layout, but the buttons are stacked vertically instead of side by side. What class is he missing?
```html
<div>
  <button class="button is-primary">Save</button>
  <button class="button is-light">Cancel</button>
</div>
```
* [ ] `has-buttons` on the `<div>`
* [x] `field is-grouped` on the `<div>` (or `buttons` class)
* [ ] `is-horizontal` on the `<div>`
* [ ] `flex-row` on the `<div>`

## In Bulma, the class `is-hidden-mobile` does what?
* [x] Hides the element only on mobile-sized screens
* [ ] Hides the element only on desktop screens
* [ ] Makes the element transparent on mobile
* [ ] Disables the element on all screen sizes

## Ana wants to show a red notification box on her page to warn users about an error. Which Bulma classes should she use?
* [ ] `class="box is-red"`
* [x] `class="notification is-danger"`
* [ ] `class="alert is-error"`
* [ ] `class="message is-warning-red"`

