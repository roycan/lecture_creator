# Quiz: AI-Assisted Development

## Maria pastes this error message into an AI assistant: `TypeError: Cannot read properties of undefined (reading 'name')`. What is the BEST follow-up action after the AI suggests a fix?
* [ ] Copy and paste the AI's code directly without reading it
* [x] Read the AI's suggestion, understand WHY it fixes the error, then test it in your own code to verify
* [ ] Ask the AI to also do your homework
* [ ] Ignore the AI and guess

## Using the C-T-C-E prompt structure (Context, Task, Constraints, Examples), which part is this? "I'm building an Express app using EJS templates and better-sqlite3."
* [ ] Task
* [x] Context — providing background about the project
* [ ] Constraints
* [ ] Examples

## Carlo asks an AI to write code for him, and the AI generates a solution using a library called "Zod" for validation. But his project uses vanilla JavaScript with manual `if`/`else` checks. What should Carlo do?
* [ ] Use Zod because the AI recommended it
* [x] Ask the AI to rewrite the solution using plain JavaScript `if`/`else` checks that match his project's stack and skill level
* [ ] Delete his project and start over with Zod
* [ ] Give up on using AI

## True or False: AI-generated code can contain bugs, outdated syntax, or security vulnerabilities, so you should always test and review it.
* [x] True
* [ ] False — AI is always correct

## Liza wants an AI to help her refactor a 200-line function. She sends just the function with the prompt "fix this." What is she missing in her prompt?
* [ ] Nothing — the AI can figure it out
* [x] Context (what the app does), the specific task (e.g., "split into smaller functions"), and constraints (e.g., "keep using vanilla JS, no new libraries")
* [ ] Her teacher's email address
* [ ] A payment

## What is a "hallucination" when using an AI coding assistant?
Answer: When the AI confidently generates code that looks correct but uses non-existent functions, APIs, or libraries that don't actually work
Answer: when AI makes up fake functions or libraries
Answer: when the AI invents things that don't exist

## Juan pastes his entire 500-line file and asks the AI "what's wrong?" The AI gives a vague answer. How can Juan get a better result?
* [ ] Paste even more code
* [x] Be specific: paste the exact error message, identify the function or line where the error occurs, and state exactly what he expected vs. what happened
* [ ] Ask the AI in a different language
* [ ] Restart the AI

## Which of these is an ETHICAL concern when using AI for coding?
* [ ] The AI runs too slowly
* [x] Submitting AI-generated code as entirely your own work without understanding it, or using AI during an exam where it's not allowed
* [ ] The AI uses too much memory
* [ ] The AI's responses are too long
