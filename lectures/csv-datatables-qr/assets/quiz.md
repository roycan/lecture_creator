# Quiz: CSV, DataTables & QR Codes

## Maria wants to display a large table of 500 student records on a web page with search and sort features. Instead of building this from scratch, which library can she use?
* [ ] React
* [x] DataTables (jQuery plugin)
* [ ] Express
* [ ] Bulma

## What does CSV stand for?
Answer: Comma-Separated Values
Answer: comma-separated values
Answer: Comma Separated Values
Answer: comma separated values

## Carlo has a CSV file with this content. How many data rows (excluding the header) does it contain?
```csv
name,price,stock
Rice,50,10
Milk,20,5
Coffee,35,8
```
* [ ] 4
* [x] 3
* [ ] 2
* [ ] 1

## Liza wants to export data from her web app into a format that users can open in Excel or Google Sheets. What file format is best for this?
* [ ] JSON
* [ ] HTML
* [x] CSV
* [ ] SQL

## When converting CSV data to display in DataTables, why is JSON often used as an intermediate format?
* [ ] JSON files are smaller than CSV files
* [x] JavaScript can parse and manipulate JSON objects more easily than raw CSV text, making it simpler to render in DataTables
* [ ] CSV is outdated and should never be used
* [ ] DataTables only accepts JSON, not CSV

## Juan wants to generate a QR code that links to a barangay event page. What type of data does a QR code typically encode?
* [ ] Only numbers
* [ ] Only images
* [x] Text (including URLs) that can be scanned by a phone camera
* [ ] Audio files

## True or False: QR codes can only contain website URLs — they cannot encode plain text or contact information.
* [ ] True
* [x] False — QR codes can encode URLs, plain text, phone numbers, Wi-Fi credentials, and more

## Ana's DataTables table loads 500 rows but is very slow. What is a common performance improvement?
* [ ] Use a smaller font
* [x] Enable server-side processing or pagination so the browser only loads a subset of rows at a time
* [ ] Remove the search feature
* [ ] Convert the table to HTML only
